---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<head>
  <link rel="canonical" href="https://github.com/gruntwork-io/knowledge-base/discussions/71" />
</head>

<CenterLayout>
<span className="searchCategory">Knowledge Base</span>
<h1>How do you organize previous AWS accounts and new ones generated by the reference architecture?</h1>
<GitHub discussion={{"id":"D_kwDOF8slf84AOLMp","number":71,"author":{"login":"marijakstrazdas"},"title":"How do you organize previous AWS accounts and new ones generated by the reference architecture?","body":"How do you organize previous AWS accounts and new ones generated by the reference architecture? \r\n\r\nHow do customers typically organize their previous AWS accounts and their new ones generated by the reference architecture? Would we be able to fold the new accounts into our current org structure or is it necessary to start from scratch, also at the org level, because we'd also get a new management account?","bodyHTML":"<p dir=\"auto\">How do you organize previous AWS accounts and new ones generated by the reference architecture?</p>\n<p dir=\"auto\">How do customers typically organize their previous AWS accounts and their new ones generated by the reference architecture? Would we be able to fold the new accounts into our current org structure or is it necessary to start from scratch, also at the org level, because we'd also get a new management account?</p>","answer":{"body":"In general, we recommend creating the new AWS accounts under the existing AWS organization rather than creating a new AWS organization for the purpose of the Reference Architecture. This way, you avoid the overhead of maintaining two root accounts and bills, while still having new fresh accounts for deploying the modules.\r\n\r\nOnce the new accounts are deployed and setup, we recommend migrating your workloads to the new accounts as much as possible. We typically recommend a migration approach as opposed to updating, due to the limitations of Terraform state migrations and imports. There are typically many resources that need to be managed and created by Terraform, and importing all of them into the state is a tedious and difficult process, especially when you are using abstractions like the Gruntwork Module library.\r\n\r\nInstead, it is oftentimes faster and easier to define new IaC configurations that deploy your existing workloads to the new account, and then update DNS records to point to the new deployments to retire the old ones when they are ready.\r\n\r\nSo in summary:\r\n\r\n- Reuse your existing AWS Organization.\r\n- But do create new AWS accounts to host the Reference Architecture.\r\n- Migrate workloads to the Reference Architecture by defining new code and infrastructure, rather than importing existing ones.","bodyHTML":"<p dir=\"auto\">In general, we recommend creating the new AWS accounts under the existing AWS organization rather than creating a new AWS organization for the purpose of the Reference Architecture. This way, you avoid the overhead of maintaining two root accounts and bills, while still having new fresh accounts for deploying the modules.</p>\n<p dir=\"auto\">Once the new accounts are deployed and setup, we recommend migrating your workloads to the new accounts as much as possible. We typically recommend a migration approach as opposed to updating, due to the limitations of Terraform state migrations and imports. There are typically many resources that need to be managed and created by Terraform, and importing all of them into the state is a tedious and difficult process, especially when you are using abstractions like the Gruntwork Module library.</p>\n<p dir=\"auto\">Instead, it is oftentimes faster and easier to define new IaC configurations that deploy your existing workloads to the new account, and then update DNS records to point to the new deployments to retire the old ones when they are ready.</p>\n<p dir=\"auto\">So in summary:</p>\n<ul dir=\"auto\">\n<li>Reuse your existing AWS Organization.</li>\n<li>But do create new AWS accounts to host the Reference Architecture.</li>\n<li>Migrate workloads to the Reference Architecture by defining new code and infrastructure, rather than importing existing ones.</li>\n</ul>"}}} />

</CenterLayout>

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "8cba857169df53d3768a6a4257544cca"
}
##DOCS-SOURCER-END -->
