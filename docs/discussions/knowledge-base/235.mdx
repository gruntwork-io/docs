---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<head>
  <link rel="canonical" href="https://github.com/gruntwork-io/knowledge-base/discussions/235" />
</head>

<CenterLayout>
<span className="searchCategory">Knowledge Base</span>
<h1>Wrapper module for Service Catalog</h1>
<GitHub discussion={{"id":"D_kwDOF8slf84AO6ME","number":235,"author":{"login":"marinalimeira"},"title":"Wrapper module for Service Catalog","body":"Hi everyone,\r\nReading the documentation about module deployment, I was wondering if having a wrapper module (i.e., a module wrapping the reference to gruntwork infrastructure library) is a best practice even when we use terragrunt with a `generate` section for both `state` and `provider`. Any thoughts or advice on this?\r\n\r\n","bodyHTML":"<p dir=\"auto\">Hi everyone,<br>\nReading the documentation about module deployment, I was wondering if having a wrapper module (i.e., a module wrapping the reference to gruntwork infrastructure library) is a best practice even when we use terragrunt with a <code class=\"notranslate\">generate</code> section for both <code class=\"notranslate\">state</code> and <code class=\"notranslate\">provider</code>. Any thoughts or advice on this?</p>","answer":{"body":"While you definitely _can_ use the IaC library modules on their own with the `generate` block, the modules can be a bit small and generic to be used on their own and you'll risk ending up with a fairly complex directory structure and dependency hierarchy in your `infrastructure-live` configuration. \r\n\r\nOne of the key benefits of using wrapper modules is composition. For example a [wrapper VPC module](https://github.com/gruntwork-io/terraform-aws-service-catalog/blob/master/modules/networking/vpc/main.tf) composes a lot of the smaller IaC library modules into a much more \"complete\" module. Another benefit of wrapper modules is ability to easily add custom `terraform` blocks, such as special IAM policies, roles, etc.","bodyHTML":"<p dir=\"auto\">While you definitely <em>can</em> use the IaC library modules on their own with the <code class=\"notranslate\">generate</code> block, the modules can be a bit small and generic to be used on their own and you'll risk ending up with a fairly complex directory structure and dependency hierarchy in your <code class=\"notranslate\">infrastructure-live</code> configuration.</p>\n<p dir=\"auto\">One of the key benefits of using wrapper modules is composition. For example a <a href=\"https://github.com/gruntwork-io/terraform-aws-service-catalog/blob/master/modules/networking/vpc/main.tf\">wrapper VPC module</a> composes a lot of the smaller IaC library modules into a much more \"complete\" module. Another benefit of wrapper modules is ability to easily add custom <code class=\"notranslate\">terraform</code> blocks, such as special IAM policies, roles, etc.</p>"}}} />

</CenterLayout>

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "8fe8da644cb93e4fe6687d9fbb156852"
}
##DOCS-SOURCER-END -->
