---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<head>
  <link rel="canonical" href="https://github.com/gruntwork-io/knowledge-base/discussions/242" />
</head>

<CenterLayout>
<span className="searchCategory">Knowledge Base</span>
<h1>What CI Server is supported in the Reference Architecture? What if I use a different CI Server - how does that get handled?</h1>
<GitHub discussion={{"id":"D_kwDOF8slf84AO6__","number":242,"author":{"login":"marijakstrazdas"},"title":"What CI Server is supported in the Reference Architecture? What if I use a different CI Server - how does that get handled?","body":"What CI Server is supported in the Reference Architecture? What if I use a different CI Server - how does that get handled? ","bodyHTML":"<p dir=\"auto\">What CI Server is supported in the Reference Architecture? What if I use a different CI Server - how does that get handled?</p>","answer":{"body":"We currently support four CI servers:\r\n\r\n* CircleCI\r\n* GitLab\r\n* Github Actions\r\n* Jenkins\r\n\r\nWhen a Reference Architecture is being provisioned for a customer, they decide which CI server they want to integrate with in their YAML form:\r\n\r\n```\r\n# TODO: Enter the CI server platform. Must be one of: circleci, jenkins, gitlab, github.\r\n# This value indicates which type of CI configuration should be generated.\r\n# NOTE: If you select Jenkins, you MUST select GitHub as the VCS platform. No other VCS is supported with Jenkins.\r\n# Example:\r\n#   CIServerPlatform: \"circleci\"\r\n```\r\n\r\nThe integration with your CI server is a separate workflow from your application CI/CD pipeline. If you want to use a different CI server for your application than your infrastructure, you can do so.","bodyHTML":"<p dir=\"auto\">We currently support four CI servers:</p>\n<ul dir=\"auto\">\n<li>CircleCI</li>\n<li>GitLab</li>\n<li>Github Actions</li>\n<li>Jenkins</li>\n</ul>\n<p dir=\"auto\">When a Reference Architecture is being provisioned for a customer, they decide which CI server they want to integrate with in their YAML form:</p>\n<div class=\"snippet-clipboard-content notranslate position-relative overflow-auto\" data-snippet-clipboard-copy-content=\"# TODO: Enter the CI server platform. Must be one of: circleci, jenkins, gitlab, github.\n# This value indicates which type of CI configuration should be generated.\n# NOTE: If you select Jenkins, you MUST select GitHub as the VCS platform. No other VCS is supported with Jenkins.\n# Example:\n#   CIServerPlatform: &quot;circleci&quot;\"><pre class=\"notranslate\"><code class=\"notranslate\"># TODO: Enter the CI server platform. Must be one of: circleci, jenkins, gitlab, github.\n# This value indicates which type of CI configuration should be generated.\n# NOTE: If you select Jenkins, you MUST select GitHub as the VCS platform. No other VCS is supported with Jenkins.\n# Example:\n#   CIServerPlatform: \"circleci\"\n</code></pre></div>\n<p dir=\"auto\">The integration with your CI server is a separate workflow from your application CI/CD pipeline. If you want to use a different CI server for your application than your infrastructure, you can do so.</p>"}}} />

</CenterLayout>

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "fc2859d5652886e02561bc51e9008415"
}
##DOCS-SOURCER-END -->
