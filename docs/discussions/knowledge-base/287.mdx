---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<head>
  <link rel="canonical" href="https://github.com/gruntwork-io/knowledge-base/discussions/287" />
</head>

<CenterLayout>
<span className="searchCategory">Knowledge Base</span>
<h1>Extending ECS Deploy Runner with custom scripts</h1>
<GitHub discussion={{"id":"D_kwDOF8slf84APBNk","number":287,"author":{"login":"yorinasub17"},"title":"Extending ECS Deploy Runner with custom scripts","body":"We are looking to add run some extra commands while running ECS Deploy Runner, like printing the IP address of the ECS Task while its running. How should I go about doing that?","bodyHTML":"<p dir=\"auto\">We are looking to add run some extra commands while running ECS Deploy Runner, like printing the IP address of the ECS Task while its running. How should I go about doing that?</p>","answer":{"body":"The best way to do this is to modify the existing trigger scripts that are installed into the docker container [here](https://github.com/gruntwork-io/terraform-aws-ci/tree/master/modules/ecs-deploy-runner/docker/deploy-runner) (those that are being put into `/opt/ecs-deploy-runner/scripts`). The nice thing about this approach is that this allows you to keep all the container and script configurations that the standard deployment supports.\r\n\r\nIf on the other hand, you would like to add new trigger scripts or additional arguments, you will need to additionally extend the container and script configurations. Specifically, the scripts need to be added to the script config created for each container [here](https://github.com/gruntwork-io/terraform-aws-ci/tree/master/modules/ecs-deploy-runner-standard-configuration).","bodyHTML":"<p dir=\"auto\">The best way to do this is to modify the existing trigger scripts that are installed into the docker container <a href=\"https://github.com/gruntwork-io/terraform-aws-ci/tree/master/modules/ecs-deploy-runner/docker/deploy-runner\">here</a> (those that are being put into <code class=\"notranslate\">/opt/ecs-deploy-runner/scripts</code>). The nice thing about this approach is that this allows you to keep all the container and script configurations that the standard deployment supports.</p>\n<p dir=\"auto\">If on the other hand, you would like to add new trigger scripts or additional arguments, you will need to additionally extend the container and script configurations. Specifically, the scripts need to be added to the script config created for each container <a href=\"https://github.com/gruntwork-io/terraform-aws-ci/tree/master/modules/ecs-deploy-runner-standard-configuration\">here</a>.</p>"}}} />

</CenterLayout>

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "d29bd6aa1b4976641d9749a661d751ce"
}
##DOCS-SOURCER-END -->
