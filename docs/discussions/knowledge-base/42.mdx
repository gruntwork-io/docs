---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<head>
  <link rel="canonical" href="https://github.com/gruntwork-io/knowledge-base/discussions/42" />
</head>

<CenterLayout>
<span className="searchCategory">Knowledge Base</span>
<h1>Where are the backend state S3 buckets for the ref arch?</h1>
<GitHub discussion={{"id":"D_kwDOF8slf84AN7zA","number":42,"author":{"login":"rhoboat"},"title":"Where are the backend state S3 buckets for the ref arch?","body":"A customer asked:\r\n\r\nWhen using the reference architecture, is it one S3 bucket in each account for the backend state, or are the buckets all kept in the same account?\r\n","bodyHTML":"<p dir=\"auto\">A customer asked:</p>\n<p dir=\"auto\">When using the reference architecture, is it one S3 bucket in each account for the backend state, or are the buckets all kept in the same account?</p>","answer":{"body":"In the Reference Architecture, the state is stored in a bucket on a per account + region basis.\r\n\r\nSo the resources deployed into the `dev` account, in the `us-west-1` region are in one S3 bucket.\r\nResources deployed into the same `dev` account, but in the `us-east-1` region are in another S3 bucket.\r\nResources deployed into the `stage` account, in the `us-west-1` region are in yet another S3 bucket.","bodyHTML":"<p dir=\"auto\">In the Reference Architecture, the state is stored in a bucket on a per account + region basis.</p>\n<p dir=\"auto\">So the resources deployed into the <code class=\"notranslate\">dev</code> account, in the <code class=\"notranslate\">us-west-1</code> region are in one S3 bucket.<br>\nResources deployed into the same <code class=\"notranslate\">dev</code> account, but in the <code class=\"notranslate\">us-east-1</code> region are in another S3 bucket.<br>\nResources deployed into the <code class=\"notranslate\">stage</code> account, in the <code class=\"notranslate\">us-west-1</code> region are in yet another S3 bucket.</p>"}}} />

</CenterLayout>

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "386d1b235ed01239aab735b4f8b8187f"
}
##DOCS-SOURCER-END -->
