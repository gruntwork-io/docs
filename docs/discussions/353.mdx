---
hide_table_of_contents: true
hide_title: true
custom_edit_url: null
---

import CenterLayout from "/src/components/CenterLayout"
import GitHub from "/src/components/GitHub"

<CenterLayout>
<span class="title">Knowledge Base</span>
<h1>How do I use the EC2 based ECS Deploy Runner?</h1>
<GitHub json={{"id":"D_kwDOF8slf84APSXP","number":353,"author":{"login":"yorinasub17"},"title":"How do I use the EC2 based ECS Deploy Runner?","body":"I have configured the ECS Deploy Runner with an EC2 worker pool, but every run is automatically being routed to Fargate. How do I use the EC2 instances?","bodyHTML":"<p dir=\"auto\">I have configured the ECS Deploy Runner with an EC2 worker pool, but every run is automatically being routed to Fargate. How do I use the EC2 instances?</p>","answer":{"body":"The default launch type to use is controlled by the `container_default_launch_type` input variable in the `ecs-deploy-runner` module, which defaults to `FARGATE`. You can update that to `EC2` to always prefer the EC2 instances.\r\n\r\nYou can also specify the launch type on a per run basis using the `--task-launch-type` option to the `infrastructure-deployer` call.","bodyHTML":"<p dir=\"auto\">The default launch type to use is controlled by the <code class=\"notranslate\">container_default_launch_type</code> input variable in the <code class=\"notranslate\">ecs-deploy-runner</code> module, which defaults to <code class=\"notranslate\">FARGATE</code>. You can update that to <code class=\"notranslate\">EC2</code> to always prefer the EC2 instances.</p>\n<p dir=\"auto\">You can also specify the launch type on a per run basis using the <code class=\"notranslate\">--task-launch-type</code> option to the <code class=\"notranslate\">infrastructure-deployer</code> call.</p>"},"comments":{"edges":[{"node":{"author":{"login":"yorinasub17"},"body":"The default launch type to use is controlled by the `container_default_launch_type` input variable in the `ecs-deploy-runner` module, which defaults to `FARGATE`. You can update that to `EC2` to always prefer the EC2 instances.\r\n\r\nYou can also specify the launch type on a per run basis using the `--task-launch-type` option to the `infrastructure-deployer` call."}}]}}} />

</CenterLayout>
  

<!-- ##DOCS-SOURCER-START
{
  "sourcePlugin": "github-discussions",
  "hash": "a99e840a3e51a528ef17c8f966e472e0"
}
##DOCS-SOURCER-END -->
